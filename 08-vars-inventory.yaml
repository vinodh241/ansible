- name: varaibles from inventory
  hosts: local
  connection: local
  tasks:
  - name: print course info
    ansible.builtin.debug:
      msg: "Hello Im learing {{COURSE}}, Course Duration:{{DURATION}},Trainer is:{{TRAINER}}"
